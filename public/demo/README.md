# Demo video drop zone

Drop your demo video in this folder named exactly:

    quai_demo_2026.mp4

The hero section auto-detects it and replaces the placeholder with a real
video player. No code changes needed — just restart `npm run dev` (or
rebuild) after adding it.
